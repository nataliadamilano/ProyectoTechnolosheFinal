@model Proyecto.Models.Genero

@{
    ViewBag.Title = "TraerArtistasPorGenero";
}

<h2>@Model.nvarchNombre</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Artistas.FirstOrDefault().nvarchNombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Artistas.FirstOrDefault().varbImagen)
        </th>
    </tr>

@foreach (var item in Model.Artistas) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nvarchNombre)
        </td>
        <td>
            @if (item.varbImagen != null)
            {
                <div class="photo-display">
                    <img class="photo-display-img" src="@Url.Action("GetImage", "Artista", new { artistaID = item.ID })" width="150" height="200" />
                </div>
            }
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
